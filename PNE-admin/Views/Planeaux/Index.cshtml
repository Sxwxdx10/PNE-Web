@model IEnumerable<PNE_core.Models.Planeau>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NiveauCouleur)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NiveauCouleur)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPlanEau">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPlanEau">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPlanEau">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
